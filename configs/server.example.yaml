grpc_address: ":50051"
shutdown_wait_sec_duration: 10s

db:
  dsn: ""
  migrations_path: "file://migrations"

auth:
  jwt_secret: ""

tls:
  cert_file: "./cert/server.crt"
  key_file: "./cert/server.key"

log:
  level: "debug"
  file: "server.log"